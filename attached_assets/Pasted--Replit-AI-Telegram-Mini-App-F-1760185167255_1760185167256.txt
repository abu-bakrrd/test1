💐 Финальный промт для Replit / AI

Создай Telegram Mini App / мобильный веб-магазин под названием Flowery Bloom — эстетичный, нежный и лёгкий онлайн-магазин цветов и открыток.

🧠 Технические требования

Backend: Python + Flask (строго ООП-архитектура).

Database: PostgreSQL (через SQLAlchemy или Flask-SQLAlchemy).

Хранение изображений: Cloudinary (в БД только URL).

Frontend: чистый HTML + CSS + JavaScript (без фреймворков, React, TypeScript, Bootstrap, Tailwind и т.д.).

Без Node.js.

Без 3D-иконок и без сложных анимаций.

Всё должно быть адаптировано под смартфоны — то есть без hover, фокус и эффекты заменяются плавными изменениями цвета или рамки.

🎨 Дизайн (максимально подробно)

Стиль: лёгкий, плоский, минималистичный, вдохновлён карточками Wildberries, но в пастельных и нежных тонах.

Основные цвета:

Фон: #FEFEFE или #FDF9F8 (молочный белый)

Основной текст: #2E2E2E

Вторичный текст: #7A7A7A

Акцент (для кнопок): #EADCF0 (нежно-розовый) или #DFF3F1 (светлый мятный)

Обводки: #EAEAEA

Шрифт: Inter, Poppins или системный sans-serif.

Форма: плавные углы (border-radius: 12–16px), минимальные тени, всё «плоское».

Без эффектов наведения и 3D-объёмов.

Всё пространство «дышит»: большие отступы, простота, фокус на товаре.

Меньше слов, больше иконок. Использовать плоские SVG: сердце, корзина, фильтр, стрелки, цена, категория, сортировка.

📱 Интерфейс (адаптация под смартфон)

Сайт рассчитан исключительно на мобильное использование (ширина контента ≤ 420 px).

Элементы интерфейса большие и удобные для нажатия (минимум 44 px по высоте).

Без горизонтальной прокрутки (только внутри блока фильтрации).

Все изображения — адаптивные, оптимизированные через Cloudinary (loading="lazy", формат WebP).

Hover-эффекты, анимации и подсветки не использовать.

🧩 Структура интерфейса
🔝 Верхняя панель

Слева — логотип и название Flowery Bloom.

Справа — иконки: ❤️ (избранное), 🛒 (корзина).

Фиксирована сверху (sticky), занимает мало места.

🎛️ Панель сортировки (в одну горизонтальную линию)

Прокручиваемая полоса под шапкой.

Включает:

Категории (из БД: розы, тюльпаны, корзины и т.д.) — в виде чипов с иконками.

Цвет — маленькие цветные кружки.

Цена от–до (два поля + иконка фильтра).

Сортировка: Новые / Старые / Дешёвые–Дорогие / Дорогие–Дешёвые.

Без выпадающих меню — всё в одной полосе, удобно для свайпа пальцем.

🌸 Каталог товаров (карточки)

Как у Wildberries: сетка карточек, 2 в ряд.

На каждой карточке:

Фото товара (главное изображение).

Слева сверху ❤️ (избранное, при нажатии заполняется цветом).

Снизу — название и цена.

Кнопка 🛒 (добавить в корзину).

При нажатии на карточку открывается страница товара.

На странице максимум 12 карточек, внизу — пагинация (← 1 2 3 →).

🔍 Страница товара

Галерея изображений (несколько фото с перелистыванием свайпом или стрелками).

Название, описание, цена.

Кнопка «Добавить в корзину» и ❤️ избранное.

Минимум текста, фокус на фото.

🛒 Корзина

Список выбранных товаров (мини-фото, название, количество, цена).

Кнопки:

Удалить товар (🗑️),

Очистить корзину,

Оформить заказ.

При нажатии «Оформить заказ» появляется модальное окно (не alert!) с полным списком заказанных товаров и суммой.
Пример содержимого:

Вы заказали:
• Розы — 3 шт. — 150 000 сум
• Тюльпаны — 2 шт. — 90 000 сум
Наш менеджер @nam свяжется с вами для оформления.
[ОК]


Модалка — светлая, плоская, без затемнения или тени, с кнопкой «ОК».

❤️ Избранное

Сетка товаров, аналогична каталогу.

Кнопка «Очистить всё» вверху.

⚙️ Backend (ООП Flask + PostgreSQL)
Основные модели:

Product: id, name, description, price, color, category_id, images (JSON), created_at.

Category: id, name.

User: id, telegram_id.

CartItem: id, user_id, product_id, quantity.

Favorite: id, user_id, product_id.

Order: id, user_id, items (JSON), total, created_at.

Основные сервисы (ООП):

ProductService (список, фильтрация, сортировка, пагинация)

CategoryService (категории)

CartService (add/remove/clear/get)

FavoriteService (add/remove/clear/get)

OrderService (создание заказа, сохранение данных)

API (возвращает JSON)

GET /api/products?page=1&per_page=12&category=roses&color=pink&sort=new

GET /api/categories

GET /api/product/<id>

POST /api/cart / GET /api/cart / DELETE /api/cart

POST /api/favorites / GET /api/favorites / DELETE /api/favorites

POST /api/order

🚫 Что запрещено

Любые 3D-иконки, кнопки с объёмом, градиенты, сложные тени.

Любые hover-эффекты.

Любые фреймворки (React, Vue, Tailwind, Bootstrap и т.д.).

Node.js, TypeScript.

Любые анимации (только мгновенная смена состояния).

🧱 Результат, который нужно получить:

Полный проект с Flask backend, HTML/CSS/JS frontend и структурой (templates, static, models, routes, services).

Полностью адаптирован под мобильные устройства.

Интерфейс — как у Wildberries, но светлый, пастельный и плоский.

Рабочие функции: фильтрация, пагинация, корзина, избранное, оформление заказа.

Модальное окно для подтверждения заказа с перечислением товаров.

Пример заполненной базы данных и инструкцию для запуска (README и .env.example).