# 🎯 Шпаргалка: Развертывание из GitHub

## 🚀 Первое развертывание (делается 1 раз)

```bash
# 1. Подключитесь к VPS
ssh root@YOUR_VPS_IP

# 2. Клонируйте ваш GitHub репозиторий
cd /opt
git clone https://github.com/ваш-username/ваш-репозиторий.git shop-deploy

# 3. Запустите автоматическую установку
cd shop-deploy
chmod +x deploy_vps.sh
./deploy_vps.sh

# 4. Откройте в браузере
# http://YOUR_VPS_IP
```

**Замените:**
- `ваш-username` → ваш GitHub username
- `ваш-репозиторий` → название репозитория

---

## 🔄 Обновление (делается каждый раз при изменениях)

### На вашем компьютере:

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте
git add .
git commit -m "Ваше описание изменений"
git push
```

### На VPS:

```bash
# 3. Подключитесь
ssh root@YOUR_VPS_IP

# 4. Обновите
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh

# Готово! Изменения применены
```

---

## ⚡ Быстрые команды

```bash
# Проверить статус
systemctl status shop-app

# Перезапустить
systemctl restart shop-app

# Посмотреть логи
journalctl -u shop-app -f

# Резервная копия БД
cd /home/shopapp/app
sudo ./backup_db.sh

# Проверить версию кода
cd /home/shopapp/app
git log --oneline -5
```

---

## 🔐 Приватный репозиторий?

Если ваш репозиторий приватный:

```bash
# На VPS создайте SSH ключ
ssh-keygen -t ed25519 -C "vps@shop"

# Выведите публичный ключ
cat ~/.ssh/id_ed25519.pub

# Скопируйте и добавьте в GitHub:
# GitHub → Settings → SSH and GPG keys → New SSH key
```

Теперь клонируйте через SSH:
```bash
git clone git@github.com:ваш-username/ваш-репозиторий.git shop-deploy
```

---

## 🆘 Если что-то пошло не так

### Конфликты при git pull

```bash
cd /home/shopapp/app

# Сбросить все локальные изменения
sudo -u shopapp git fetch origin
sudo -u shopapp git reset --hard origin/main

# Обновить
sudo ./update_vps.sh
```

### Приложение не запускается

```bash
# Посмотреть логи
journalctl -u shop-app -n 100

# Проверить статус
systemctl status shop-app
systemctl status nginx
systemctl status postgresql
```

### Откатить на предыдущую версию

```bash
cd /home/shopapp/app

# Посмотреть историю коммитов
git log --oneline -10

# Откатиться на нужный коммит (замените abc123 на хеш коммита)
sudo -u shopapp git checkout abc123

# Обновить
sudo ./update_vps.sh

# Вернуться на последнюю версию
sudo -u shopapp git checkout main
sudo ./update_vps.sh
```

---

## 📝 Полезные проверки

```bash
# Какая версия сейчас на сервере?
cd /home/shopapp/app
git log -1 --pretty=format:"%h - %s (%cr)"

# Какие файлы изменены локально?
git status

# Проверить удаленный репозиторий
git remote -v

# Проверить, есть ли обновления
git fetch
git status
```

---

## 🎓 Рекомендуемый workflow

```
1. Разработка локально
   ↓
2. git add . && git commit -m "..." && git push
   ↓
3. ssh root@YOUR_VPS_IP
   ↓
4. cd /home/shopapp/app
   ↓
5. sudo -u shopapp git pull && sudo ./update_vps.sh
   ↓
6. Проверить в браузере: http://YOUR_VPS_IP
```

---

**Вопросы? См. DEPLOY_FROM_GITHUB.md для подробностей**
