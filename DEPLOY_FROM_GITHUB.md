# 🚀 Развертывание из GitHub на VPS

## Быстрый старт (2 команды!)

### Шаг 1: Подключитесь к VPS

```bash
ssh root@81.162.55.47
```

### Шаг 2: Клонируйте и запустите установку

```bash
# Клонируйте ваш репозиторий
cd /opt
git clone https://github.com/ваш-username/ваш-репозиторий.git monvoir-deploy

# Перейдите в директорию
cd monvoir-deploy

# Запустите автоматическую установку
chmod +x deploy_vps.sh
./deploy_vps.sh
```

**Готово!** 🎉

Скрипт автоматически:
- ✅ Установит PostgreSQL
- ✅ Установит все зависимости
- ✅ Соберет фронтенд
- ✅ Настроит Nginx
- ✅ Запустит приложение

После завершения откройте: **http://81.162.55.47**

---

## 🔄 Обновление приложения из GitHub

Когда вы обновите код в GitHub:

```bash
# Подключитесь к VPS
ssh root@81.162.55.47

# Перейдите в директорию приложения
cd /home/monvoir/app

# Получите обновления из GitHub
sudo -u monvoir git pull

# Запустите скрипт обновления
sudo ./update_vps.sh
```

Скрипт автоматически:
- ✅ Сделает резервную копию БД
- ✅ Установит новые зависимости
- ✅ Соберет фронтенд
- ✅ Перезапустит приложение

---

## 🔐 Работа с приватными репозиториями

Если ваш репозиторий приватный, есть два варианта:

### Вариант 1: SSH ключ (Рекомендуется)

```bash
# На VPS создайте SSH ключ
ssh-keygen -t ed25519 -C "vps@monvoir"

# Выведите публичный ключ
cat ~/.ssh/id_ed25519.pub

# Скопируйте этот ключ и добавьте в GitHub:
# GitHub → Settings → SSH and GPG keys → New SSH key
```

Теперь клонируйте через SSH:
```bash
git clone git@github.com:ваш-username/ваш-репозиторий.git monvoir-deploy
```

### Вариант 2: Personal Access Token

```bash
# Создайте токен в GitHub:
# GitHub → Settings → Developer settings → Personal access tokens → Generate new token

# Клонируйте с токеном:
git clone https://ваш-токен@github.com/ваш-username/ваш-репозиторий.git monvoir-deploy
```

---

## 📝 Модифицированный скрипт автоматической установки

Скрипт `deploy_vps.sh` уже поддерживает работу с Git! Он автоматически:
- Определит, что код клонирован из Git
- Сохранит связь с репозиторием
- Позволит легко обновлять код через `git pull`

---

## 🎯 Полная последовательность действий

### 1. Подготовка репозитория (один раз)

Убедитесь, что в вашем GitHub репозитории есть:
- ✅ `deploy_vps.sh`
- ✅ `update_vps.sh`
- ✅ `backup_db.sh`
- ✅ `restore_db.sh`
- ✅ `.env.example`
- ✅ Все файлы проекта

### 2. Развертывание на VPS (один раз)

```bash
# Подключитесь к VPS
ssh root@81.162.55.47

# Клонируйте репозиторий
cd /opt
git clone https://github.com/ваш-username/ваш-репозиторий.git monvoir-deploy
cd monvoir-deploy

# Запустите установку
chmod +x *.sh
./deploy_vps.sh
```

### 3. Разработка и обновление (постоянно)

```bash
# На вашем компьютере:
# 1. Внесите изменения в код
# 2. Закоммитьте и запуште в GitHub
git add .
git commit -m "Update: описание изменений"
git push

# На VPS:
# 3. Получите обновления и перезапустите
ssh root@81.162.55.47
cd /home/monvoir/app
sudo -u monvoir git pull
sudo ./update_vps.sh
```

---

## 🔧 Настройка автоматического обновления (опционально)

Можно настроить автоматическое обновление при push в GitHub через Webhooks:

### Шаг 1: Создайте скрипт webhook

```bash
# На VPS создайте скрипт
sudo nano /home/monvoir/webhook_update.sh
```

Содержимое:
```bash
#!/bin/bash
cd /home/monvoir/app
sudo -u monvoir git pull
/home/monvoir/app/update_vps.sh
```

```bash
# Сделайте исполняемым
sudo chmod +x /home/monvoir/webhook_update.sh
```

### Шаг 2: Настройте простой webhook сервер

Или используйте готовые решения типа:
- webhook (https://github.com/adnanh/webhook)
- GitHub Actions для деплоя

---

## 🌿 Работа с ветками

### Production ветка

```bash
# На VPS переключитесь на production ветку
cd /home/monvoir/app
sudo -u monvoir git checkout production
sudo ./update_vps.sh
```

### Development ветка для тестирования

Можно создать отдельное окружение для тестирования:

```bash
# Клонируйте в отдельную директорию
cd /opt
git clone -b development https://github.com/ваш-username/ваш-репозиторий.git monvoir-dev

# Настройте на другом порту (например, 5001)
# И используйте отдельную БД
```

---

## 📊 .gitignore

Убедитесь, что в вашем `.gitignore` есть:

```gitignore
# Environment
.env
.env.local

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Node
node_modules/
dist/
build/

# Logs
*.log
logs/

# Backups
backups/
*.sql
*.sql.gz

# IDE
.vscode/
.idea/
*.swp
```

---

## 🔍 Проверка статуса Git на VPS

```bash
# Проверить текущую ветку и статус
cd /home/monvoir/app
git branch
git status

# Посмотреть историю коммитов
git log --oneline -10

# Посмотреть изменения
git diff

# Проверить удаленный репозиторий
git remote -v
```

---

## 🆘 Решение проблем с Git

### Конфликты при git pull

```bash
# Если есть локальные изменения, сохраните их
cd /home/monvoir/app
sudo -u monvoir git stash

# Получите обновления
sudo -u monvoir git pull

# Примените сохраненные изменения (если нужно)
sudo -u monvoir git stash pop
```

### Сброс до состояния в GitHub

```bash
# ВНИМАНИЕ: Это удалит все локальные изменения!
cd /home/monvoir/app
sudo -u monvoir git fetch origin
sudo -u monvoir git reset --hard origin/main  # или origin/master
sudo ./update_vps.sh
```

---

## ✅ Преимущества развертывания из GitHub

✅ **Быстрые обновления** - один `git pull` вместо загрузки файлов  
✅ **История изменений** - всегда видно, что и когда изменилось  
✅ **Легкий откат** - можно вернуться к любой версии  
✅ **Автоматизация** - можно настроить CI/CD  
✅ **Командная работа** - несколько человек могут работать над кодом  

---

## 🎓 Рекомендуемый workflow

```bash
# 1. Разработка на локальном компьютере
git checkout -b feature/new-feature
# ... делаете изменения ...
git add .
git commit -m "Add new feature"
git push origin feature/new-feature

# 2. Создаете Pull Request на GitHub
# 3. Ревьюите и мержите в main

# 4. На VPS обновляете production
ssh root@81.162.55.47
cd /home/monvoir/app
sudo -u monvoir git checkout main
sudo -u monvoir git pull
sudo ./update_vps.sh
```

---

**Теперь ваше развертывание полностью интегрировано с GitHub! 🚀**
