# 🤖 Сборка Telegram бота в EXE файл для Windows

Эта инструкция поможет вам собрать Telegram бота для управления товарами в исполняемый `.exe` файл, который можно запускать на Windows без установки Python.

## 📋 Требования

- **Windows** 10/11
- **Python** 3.8 или новее ([скачать здесь](https://www.python.org/downloads/))
- Скачанная и распакованная папка `telegram_bot`

## ⚡ Быстрый старт

### Вариант 1: Автоматическая сборка (рекомендуется)

1. Откройте папку `telegram_bot`
2. Дважды кликните на файл **`build_exe.bat`**
3. Дождитесь завершения сборки (3-5 минут)
4. Готово! EXE файл будет в папке `dist/`

### Вариант 2: Ручная сборка

Откройте командную строку в папке `telegram_bot` и выполните:

```bash
# Установка зависимостей
pip install pyinstaller
pip install -r requirements.txt

# Сборка
pyinstaller bot.spec --clean --noconfirm
```

## 📦 Что получится после сборки

После успешной сборки в папке `dist/` появится:

```
dist/
├── TelegramShopBot.exe   # Исполняемый файл бота
├── .env                  # Файл с настройками (скопируется автоматически)
└── settingsbot.json      # Файл с категориями (скопируется автоматически)
```

## 🚀 Запуск бота

### Перед первым запуском:

1. Убедитесь, что файлы `.env` и `settingsbot.json` находятся в папке `dist/` рядом с exe файлом
2. Проверьте, что в файле `.env` указаны все необходимые данные:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `DATABASE_URL` - подключение к базе данных
   - `CLOUDINARY_*` - данные Cloudinary

### Запуск:

1. Откройте папку `dist/`
2. Дважды кликните на `TelegramShopBot.exe`
3. Откроется консольное окно с ботом
4. Бот готов к работе!

## 🔧 Настройка

### Файл `.env`

Пример содержимого:

```env
DATABASE_URL=postgresql://user:password@your-vps-ip:5432/shop_db
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=123456789012345
CLOUDINARY_API_SECRET=abcdefghijklmnopqrstuvwxyz
```

### Файл `settingsbot.json`

Пример содержимого:

```json
{
  "authorized_users": [
    123456789
  ],
  "categories": [
    {
      "id": "category-1",
      "name": "Категория 1"
    }
  ]
}
```

## ❓ Частые вопросы

### Бот не запускается

1. **Проверьте файл `.env`**: Все токены должны быть заполнены
2. **Проверьте подключение к базе данных**: DATABASE_URL должен быть доступен
3. **Откройте консоль**: Запустите exe через командную строку чтобы увидеть ошибки

### Как обновить бота

1. Скачайте новую версию папки `telegram_bot` с VPS
2. Запустите `build_exe.bat` заново
3. Скопируйте старые `.env` и `settingsbot.json` в новую папку `dist/`

### Антивирус блокирует exe файл

Это нормально для файлов собранных PyInstaller. Добавьте exe в исключения антивируса.

### Как узнать свой Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Добавьте его в `settingsbot.json` в массив `authorized_users`

## 📝 Структура файлов

```
telegram_bot/
├── bot.py                 # Основной файл бота
├── db_operations.py       # Работа с базой данных
├── requirements.txt       # Зависимости Python
├── settingsbot.json       # Настройки бота
├── .env                   # Переменные окружения
├── bot.spec              # Конфигурация PyInstaller
├── build_exe.bat         # Скрипт автоматической сборки
└── README_BUILD.md       # Эта инструкция
```

## 🆘 Помощь

Если возникли проблемы:

1. Проверьте что Python установлен: `python --version`
2. Проверьте что все зависимости установлены: `pip list`
3. Убедитесь что `.env` файл заполнен правильно
4. Проверьте подключение к базе данных на VPS

---

**Успешной сборки! 🚀**
