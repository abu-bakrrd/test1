# 🤖 Настройка Telegram бота для управления товарами

## Что делает бот?

Telegram бот позволяет добавлять, удалять и управлять товарами в базе данных через Telegram без доступа к серверу.

---

## 🔗 Подключение к базе данных на VPS

### Вариант 1: Бот запускается НА VPS (рекомендуется) ⭐

Если бот работает на том же VPS, где и магазин - самый простой способ.

#### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP
```

#### Шаг 2: Получите DATABASE_URL

```bash
# Посмотрите файл .env
cat /home/shopapp/app/.env
```

Вы увидите что-то вроде:
```
DATABASE_URL=postgresql://shop_user:ВАШ_ПАРОЛЬ@localhost:5432/shop_db
PORT=5000
FLASK_ENV=production
```

**Скопируйте всю строку DATABASE_URL** - это и есть ссылка на базу данных!

#### Шаг 3: Настройте бота на VPS

```bash
# Перейдите в директорию бота
cd /home/shopapp/app/telegram_bot

# Создайте .env файл для бота
nano .env
```

Вставьте:
```bash
# База данных (скопируйте из /home/shopapp/app/.env)
DATABASE_URL=postgresql://shop_user:ВАШ_ПАРОЛЬ@localhost:5432/shop_db

# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather

# Cloudinary (для загрузки фото товаров)
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret
```

Сохраните: `Ctrl+O`, Enter, `Ctrl+X`

#### Шаг 4: Настройте авторизацию

Узнайте свой Telegram ID:
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он пришлёт ваш ID (например: `123456789`)

Добавьте свой ID в настройки:
```bash
nano settingsbot.json
```

Измените:
```json
{
  "authorized_users": [
    123456789
  ],
  "categories": [...]
}
```

#### Шаг 5: Установите зависимости

```bash
# Создайте виртуальное окружение
python3 -m venv venv_bot
source venv_bot/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

#### Шаг 6: Запустите бота

```bash
python3 bot.py
```

Бот должен запуститься и написать:
```
✅ Cloudinary настроен успешно
🤖 Бот запущен!
```

**Готово!** Напишите боту `/start` в Telegram.

---

### Вариант 2: Бот запускается НА ВАШЕМ КОМПЬЮТЕРЕ

Если хотите запускать бота с компьютера, нужно настроить внешний доступ к базе данных.

#### Шаг 1: Откройте PostgreSQL для внешних подключений

На VPS:

```bash
# Откройте конфигурацию PostgreSQL
PG_VERSION=$(ls /etc/postgresql/)
sudo nano /etc/postgresql/$PG_VERSION/main/postgresql.conf
```

Найдите строку:
```
#listen_addresses = 'localhost'
```

Измените на:
```
listen_addresses = '*'
```

Сохраните и закройте.

#### Шаг 2: Разрешите подключения

```bash
# Откройте pg_hba.conf
sudo nano /etc/postgresql/$PG_VERSION/main/pg_hba.conf
```

Добавьте в конец:
```
# Разрешить подключения из любого места (или укажите конкретный IP)
host    all             all             0.0.0.0/0               md5
```

**Внимание:** `0.0.0.0/0` разрешает подключения отовсюду. Для безопасности лучше указать конкретный IP:
```
host    all             all             ВАШ_IP/32               md5
```

Сохраните.

#### Шаг 3: Откройте порт в Firewall

```bash
# Откройте порт PostgreSQL (5432)
sudo ufw allow 5432/tcp

# Проверьте
sudo ufw status
```

#### Шаг 4: Перезапустите PostgreSQL

```bash
sudo systemctl restart postgresql
```

#### Шаг 5: Получите DATABASE_URL для внешнего подключения

На VPS:
```bash
cat /home/shopapp/app/.env
```

**Измените `localhost` на IP адрес VPS:**

Было:
```
DATABASE_URL=postgresql://shop_user:пароль@localhost:5432/shop_db
```

Стало:
```
DATABASE_URL=postgresql://shop_user:пароль@ВАШ_IP_VPS:5432/shop_db
```

#### Шаг 6: На вашем компьютере

Создайте папку для бота:
```bash
mkdir telegram_bot
cd telegram_bot
```

Скопируйте файлы бота с VPS или из репозитория:
- `bot.py`
- `db_operations.py`
- `requirements.txt`
- `settingsbot.json`

Создайте `.env`:
```bash
# База данных (обратите внимание - IP адрес VPS вместо localhost!)
DATABASE_URL=postgresql://shop_user:ВАШ_ПАРОЛЬ@ВАШ_IP_VPS:5432/shop_db

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=ваш_токен

# Cloudinary
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret
```

Установите зависимости:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

Запустите:
```bash
python3 bot.py
```

---

## 🔑 Получение токена бота

### Шаг 1: Создайте бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Напишите `/newbot`
4. Введите имя бота (например: `Мой Магазин Бот`)
5. Введите username (например: `my_shop_bot`)

### Шаг 2: Скопируйте токен

BotFather пришлёт токен:
```
123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Скопируйте его** и добавьте в `.env`:
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

## 📸 Настройка Cloudinary (для фото товаров)

Бот загружает фото товаров в Cloudinary.

### Шаг 1: Зарегистрируйтесь

1. Откройте [cloudinary.com](https://cloudinary.com)
2. Зарегистрируйтесь (бесплатно)
3. Подтвердите email

### Шаг 2: Получите данные

1. Откройте Dashboard
2. Найдите:
   - **Cloud Name**
   - **API Key**
   - **API Secret**

### Шаг 3: Добавьте в .env

```bash
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret
```

---

## 👤 Авторизация пользователей

Только авторизованные пользователи могут управлять товарами.

### Узнайте свой Telegram ID:

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он пришлёт ваш ID

### Добавьте в settingsbot.json:

```json
{
  "authorized_users": [
    123456789,
    987654321
  ],
  "categories": [...]
}
```

Можете добавить несколько пользователей.

---

## 📁 Категории товаров

Категории настраиваются в `settingsbot.json`:

```json
{
  "authorized_users": [123456789],
  "categories": [
    {
      "id": "electronics",
      "name": "Электроника"
    },
    {
      "id": "clothing",
      "name": "Одежда"
    },
    {
      "id": "food",
      "name": "Продукты"
    }
  ]
}
```

**Важно:** ID категорий должны совпадать с теми, что вы используете в основной базе данных!

---

## 🚀 Запуск бота как сервиса (на VPS)

Чтобы бот работал постоянно в фоне:

### Создайте systemd сервис

```bash
sudo nano /etc/systemd/system/shop-bot.service
```

Вставьте:
```ini
[Unit]
Description=Telegram Shop Bot
After=network.target postgresql.service

[Service]
Type=simple
User=shopapp
WorkingDirectory=/home/shopapp/app/telegram_bot
Environment="PATH=/home/shopapp/app/telegram_bot/venv_bot/bin"
ExecStart=/home/shopapp/app/telegram_bot/venv_bot/bin/python3 bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Запустите сервис

```bash
# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable shop-bot

# Запустите
sudo systemctl start shop-bot

# Проверьте статус
sudo systemctl status shop-bot

# Посмотрите логи
journalctl -u shop-bot -f
```

Теперь бот будет работать всегда, даже после перезагрузки VPS!

---

## 🔧 Использование бота

### Команды:

- `/start` - Начать работу
- **➕ Добавить товар** - Добавить новый товар
- **🗑 Удалить товар** - Удалить товар
- **📋 Список товаров** - Показать все товары
- **📁 Категории** - Показать категории

### Процесс добавления товара:

1. Нажмите "➕ Добавить товар"
2. Выберите категорию
3. Введите название товара
4. Введите описание
5. Введите цену (в рублях/долларах)
6. Отправьте фото товара

Товар автоматически добавится в базу данных!

---

## 🆘 Решение проблем

### Бот не подключается к БД

```bash
# Проверьте DATABASE_URL
echo $DATABASE_URL

# Проверьте что PostgreSQL работает
systemctl status postgresql

# Попробуйте подключиться вручную
psql $DATABASE_URL
```

### Ошибка "Database connection failed"

**На VPS:**
```bash
# Проверьте что база существует
sudo -u postgres psql -c "\l" | grep shop_db

# Проверьте пользователя
sudo -u postgres psql -c "\du" | grep shop_user
```

**С компьютера:**
Проверьте:
1. Открыт ли порт 5432 в firewall
2. Правильный ли IP адрес в DATABASE_URL
3. Разрешены ли внешние подключения в PostgreSQL

### Cloudinary не работает

Проверьте:
```bash
# Переменные окружения
echo $CLOUDINARY_CLOUD_NAME
echo $CLOUDINARY_API_KEY
```

Убедитесь что данные правильные на cloudinary.com → Dashboard

### "Unauthorized user"

Проверьте:
1. Ваш ID добавлен в `settingsbot.json`
2. ID правильный (напишите @userinfobot)
3. Перезапустите бота после изменения настроек

---

## 📊 Сравнение вариантов запуска

| Параметр | На VPS | На компьютере |
|----------|--------|---------------|
| **Скорость** | Быстро | Зависит от интернета |
| **Доступность** | 24/7 | Только когда компьютер включён |
| **Безопасность** | ✅ Высокая | ⚠️ Нужно открывать порты |
| **Настройка** | Легче | Сложнее |

**Рекомендуем:** Запускать на VPS

---

## ✅ Чек-лист настройки

- [ ] Получил токен от @BotFather
- [ ] Зарегистрировался на Cloudinary
- [ ] Узнал свой Telegram ID
- [ ] Создал .env с DATABASE_URL
- [ ] Добавил себя в authorized_users
- [ ] Установил зависимости
- [ ] Запустил бота
- [ ] Написал боту /start
- [ ] Добавил тестовый товар
- [ ] (Опционально) Настроил systemd сервис

---

## 📝 Пример полного .env

```bash
# === База данных ===
# На VPS (localhost)
DATABASE_URL=postgresql://shop_user:мой_пароль@localhost:5432/shop_db

# С компьютера (IP адрес VPS)
# DATABASE_URL=postgresql://shop_user:мой_пароль@123.45.67.89:5432/shop_db

# === Telegram ===
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# === Cloudinary ===
CLOUDINARY_CLOUD_NAME=my_cloud
CLOUDINARY_API_KEY=123456789012345
CLOUDINARY_API_SECRET=abcdefghijklmnopqrstuvwxyz
```

---

**Успешной настройки бота! 🤖**

Вопросы? → [ПОЛНОЕ_РУКОВОДСТВО.md](ПОЛНОЕ_РУКОВОДСТВО.md)
