# 📦 Сводка по развертыванию на VPS

## ✅ Что создано для вас

Я подготовил полный набор файлов и скриптов для развертывания вашего Telegram Mini App на VPS Ubuntu 22.04.

---

## 📁 Созданные файлы

### 📘 Документация

1. **QUICK_START_RU.md** - Быстрый старт (начните с этого!)
2. **VPS_DEPLOY_GUIDE.md** - Полная подробная инструкция
3. **DEPLOY_TO_VPS_README.md** - Справочник команд и решение проблем
4. **VPS_DEPLOYMENT_SUMMARY.md** - Этот файл

### 🔧 Скрипты автоматизации

1. **deploy_vps.sh** - Автоматическое развертывание (главный скрипт)
2. **update_vps.sh** - Обновление приложения
3. **backup_db.sh** - Резервное копирование базы данных
4. **restore_db.sh** - Восстановление базы данных

### ⚙️ Конфигурация

1. **.env.example** - Пример файла переменных окружения

---

## 🎯 Что делать дальше?

### Вариант 1: Автоматическое развертывание (Рекомендуется)

```bash
# 1. На вашем компьютере - создайте архив
tar -czf monvoir.tar.gz .

# 2. Загрузите на VPS
scp monvoir.tar.gz root@81.162.55.47:/root/

# 3. Подключитесь к VPS
ssh root@81.162.55.47

# 4. Распакуйте и запустите установку
mkdir -p /opt/monvoir-deploy
cd /opt/monvoir-deploy
tar -xzf /root/monvoir.tar.gz
chmod +x deploy_vps.sh
./deploy_vps.sh
```

### Вариант 2: Ручная установка

Следуйте инструкциям в файле `VPS_DEPLOY_GUIDE.md`

---

## 🏗️ Что будет установлено автоматически

Скрипт `deploy_vps.sh` установит и настроит:

### Системные пакеты
- ✅ Python 3 + pip + venv
- ✅ Node.js (LTS) + npm
- ✅ PostgreSQL 14+
- ✅ Nginx
- ✅ Git
- ✅ UFW Firewall

### База данных
- ✅ Создаст пользователя БД
- ✅ Создаст базу данных
- ✅ Настроит права доступа
- ✅ Инициализирует таблицы
- ✅ Загрузит тестовые данные (по желанию)

### Приложение
- ✅ Создаст системного пользователя
- ✅ Установит Python зависимости (Flask, Gunicorn, psycopg2)
- ✅ Установит Node.js зависимости
- ✅ Соберет React фронтенд
- ✅ Настроит systemd сервис
- ✅ Запустит приложение

### Веб-сервер
- ✅ Настроит Nginx как reverse proxy
- ✅ Настроит кеширование статических файлов
- ✅ Настроит логирование

### Безопасность
- ✅ Настроит UFW Firewall
- ✅ Разрешит только необходимые порты (SSH, HTTP, HTTPS)
- ✅ Настроит безопасное хранение паролей БД

---

## 📊 Архитектура развертывания

```
Интернет
    ↓
Nginx (порт 80/443) ← Firewall (UFW)
    ↓
Flask + Gunicorn (порт 5000)
    ↓
PostgreSQL (порт 5432, localhost)
```

### Структура на сервере

```
/home/monvoir/
└── app/
    ├── venv/              # Python виртуальное окружение
    ├── dist/              # Собранный фронтенд
    │   └── public/
    ├── config/            # Конфигурация магазина
    ├── backups/           # Резервные копии БД
    ├── app.py             # Flask приложение
    ├── .env               # Переменные окружения
    └── [остальные файлы]
```

---

## 🔐 Безопасность

### Что настроено автоматически

✅ Firewall (UFW) - разрешены только HTTP, HTTPS, SSH  
✅ PostgreSQL доступен только локально  
✅ Пароли БД в защищенном .env файле (права 600)  
✅ Приложение работает от непривилегированного пользователя  
✅ Production сервер (Gunicorn) вместо Flask dev server  

### Рекомендации после установки

- [ ] Смените SSH порт с 22 на нестандартный
- [ ] Настройте SSH ключи вместо паролей
- [ ] Установите fail2ban для защиты от брутфорса
- [ ] Настройте SSL сертификат (Let's Encrypt)
- [ ] Настройте автоматические обновления безопасности
- [ ] Настройте регулярное резервное копирование

---

## 🔄 Обслуживание

### Обновление приложения

```bash
cd /home/monvoir/app
sudo ./update_vps.sh
```

### Резервное копирование

```bash
# Создать резервную копию
sudo ./backup_db.sh

# Восстановить из резервной копии
sudo ./restore_db.sh
```

### Мониторинг

```bash
# Статус приложения
systemctl status monvoir-app

# Логи приложения
journalctl -u monvoir-app -f

# Логи Nginx
tail -f /var/log/nginx/monvoir_error.log

# Использование ресурсов
htop
```

---

## 📈 Производительность

### Настройки Gunicorn

По умолчанию настроено:
- **Workers**: 4 (рекомендуется: 2-4 × количество CPU ядер)
- **Timeout**: 120 секунд
- **Bind**: 127.0.0.1:5000 (только локальный доступ)

### Кеширование

Nginx кеширует статические файлы:
- `/assets/*` - 1 год
- `/config/*` - 1 час

---

## 🌐 Доступ к приложению

После развертывания:

- **HTTP**: http://81.162.55.47
- **Telegram Mini App**: Настройте URL в BotFather

### Настройка SSL (опционально)

Если у вас есть домен:

```bash
# Установить Certbot
sudo apt install certbot python3-certbot-nginx

# Получить сертификат
sudo certbot --nginx -d yourdomain.com
```

После этого:
- **HTTPS**: https://yourdomain.com

---

## 🆘 Решение проблем

### Частые проблемы и решения

| Проблема | Решение |
|----------|---------|
| Приложение не открывается | `systemctl status monvoir-app`, проверить логи |
| 502 Bad Gateway | Проверить, запущен ли Flask: `systemctl status monvoir-app` |
| База данных не подключается | `systemctl status postgresql`, проверить .env |
| Нет места на диске | `df -h`, очистить старые логи и бэкапы |
| Высокая нагрузка | `htop`, увеличить количество workers в Gunicorn |

Подробнее см. раздел "Решение проблем" в `DEPLOY_TO_VPS_README.md`

---

## 📞 Полезные ссылки

### Основные файлы конфигурации

- Systemd сервис: `/etc/systemd/system/monvoir-app.service`
- Nginx конфигурация: `/etc/nginx/sites-available/monvoir`
- Переменные окружения: `/home/monvoir/app/.env`
- PostgreSQL настройки: `/etc/postgresql/14/main/postgresql.conf`

### Логи

- Приложение: `journalctl -u monvoir-app`
- Nginx доступ: `/var/log/nginx/monvoir_access.log`
- Nginx ошибки: `/var/log/nginx/monvoir_error.log`
- PostgreSQL: `/var/log/postgresql/postgresql-14-main.log`

---

## ✨ Дополнительные возможности

### Telegram Bot

Если вам нужен Telegram бот, см. раздел "Настройка Telegram Bot" в `VPS_DEPLOY_GUIDE.md`

### Автоматическое резервное копирование

```bash
# Добавить в cron для ежедневного бэкапа в 3:00
sudo crontab -e

# Добавить строку:
0 3 * * * /home/monvoir/app/backup_db.sh >> /var/log/monvoir-backup.log 2>&1
```

### Мониторинг производительности

Установите инструменты мониторинга:

```bash
# Установить htop
sudo apt install htop

# Установить iotop (мониторинг дисковых операций)
sudo apt install iotop

# Установить nethogs (мониторинг сетевого трафика)
sudo apt install nethogs
```

---

## 🎓 Рекомендуемый порядок действий

1. **Изучите** `QUICK_START_RU.md` - быстрый старт
2. **Выполните** развертывание с помощью `deploy_vps.sh`
3. **Проверьте** работу приложения в браузере
4. **Настройте** SSL сертификат (если есть домен)
5. **Настройте** автоматическое резервное копирование
6. **Изучите** `DEPLOY_TO_VPS_README.md` для дальнейшего управления

---

## 📋 Чеклист развертывания

### Перед развертыванием

- [ ] У вас есть доступ к VPS по SSH
- [ ] VPS на Ubuntu 22.04
- [ ] У вас есть права root или sudo
- [ ] Файлы проекта готовы к загрузке

### Во время развертывания

- [ ] Загрузили файлы на VPS
- [ ] Запустили скрипт deploy_vps.sh
- [ ] Ввели пароль для БД
- [ ] Дождались завершения установки

### После развертывания

- [ ] Приложение доступно по IP
- [ ] База данных работает
- [ ] Логи не показывают ошибок
- [ ] Создана первая резервная копия
- [ ] Firewall настроен
- [ ] SSL настроен (если нужен)
- [ ] Настроено автоматическое резервное копирование

---

## 🎉 Готово!

Все необходимые файлы и документация созданы. Теперь вы можете развернуть ваше приложение на VPS за несколько минут!

**Начните с файла: `QUICK_START_RU.md`**

---

*Дата создания: 07 ноября 2025*  
*VPS IP: 81.162.55.47*  
*OS: Ubuntu 22.04*
