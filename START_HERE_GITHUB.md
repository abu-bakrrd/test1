# 👋 Начните отсюда - Развертывание из GitHub

## ✅ У вас есть GitHub репозиторий - это отлично!

Развертывание будет максимально простым: всего **2 команды**!

---

## 🎯 Что делать прямо сейчас

### 1. Убедитесь, что эти файлы есть в вашем GitHub репозитории

Проверьте, что у вас закоммичены:
- ✅ `deploy_vps.sh`
- ✅ `update_vps.sh`
- ✅ `backup_db.sh`
- ✅ `restore_db.sh`
- ✅ `.env.example`
- ✅ Вся документация (MD файлы)

Если нет - закоммитьте их:

```bash
git add .
git commit -m "Add VPS deployment scripts and documentation"
git push
```

### 2. Подключитесь к VPS и развернуть приложение

```bash
# Подключитесь к VPS
ssh root@81.162.55.47

# Клонируйте ваш репозиторий (ЗАМЕНИТЕ на ваш URL!)
cd /opt
git clone https://github.com/ваш-username/ваш-репозиторий.git monvoir-deploy

# Запустите установку
cd monvoir-deploy
chmod +x deploy_vps.sh
./deploy_vps.sh
```

**Скрипт всё сделает автоматически!** Вас только попросят ввести пароль для базы данных.

### 3. Готово! 🎉

Откройте в браузере: **http://81.162.55.47**

---

## 📖 Какой файл читать дальше?

| Файл | Для чего |
|------|----------|
| **GITHUB_DEPLOY_CHEATSHEET.md** | Быстрая шпаргалка для работы с GitHub |
| **QUICK_START_RU.md** | Быстрый старт и основные команды |
| **DEPLOY_FROM_GITHUB.md** | Подробная инструкция по работе с GitHub |
| **VPS_DEPLOY_GUIDE.md** | Полное руководство со всеми деталями |

---

## 🔄 Как обновлять приложение после изменений

**На вашем компьютере:**
```bash
git add .
git commit -m "Ваши изменения"
git push
```

**На VPS:**
```bash
ssh root@81.162.55.47
cd /home/monvoir/app
sudo -u monvoir git pull
sudo ./update_vps.sh
```

Всё! Изменения применены.

---

## 🗂️ Структура файлов документации

```
📁 Ваш репозиторий
│
├── 🌟 START_HERE_GITHUB.md          ← ВЫ ЗДЕСЬ
├── 📋 GITHUB_DEPLOY_CHEATSHEET.md   ← Быстрая шпаргалка
├── 🚀 QUICK_START_RU.md             ← Быстрый старт
├── 🐙 DEPLOY_FROM_GITHUB.md         ← Работа с GitHub
├── 📚 VPS_DEPLOY_GUIDE.md           ← Полное руководство
├── 📖 DEPLOY_TO_VPS_README.md       ← Справочник команд
├── 📊 VPS_DEPLOYMENT_SUMMARY.md     ← Техническая сводка
│
├── 🔧 deploy_vps.sh                 ← Автоматическая установка
├── 🔄 update_vps.sh                 ← Обновление приложения
├── 💾 backup_db.sh                  ← Резервная копия БД
├── 🔙 restore_db.sh                 ← Восстановление БД
└── ⚙️  .env.example                  ← Пример конфигурации
```

---

## ⚡ Быстрые ответы на частые вопросы

### У меня приватный репозиторий - что делать?

Создайте SSH ключ на VPS и добавьте его в GitHub:

```bash
ssh root@81.162.55.47
ssh-keygen -t ed25519 -C "vps@monvoir"
cat ~/.ssh/id_ed25519.pub
# Скопируйте вывод и добавьте в GitHub → Settings → SSH keys
```

Затем клонируйте через SSH:
```bash
git clone git@github.com:ваш-username/ваш-репозиторий.git monvoir-deploy
```

### Как узнать, какая версия сейчас на сервере?

```bash
ssh root@81.162.55.47
cd /home/monvoir/app
git log -1 --pretty=format:"%h - %s (%cr)"
```

### Как откатить изменения, если что-то сломалось?

```bash
ssh root@81.162.55.47
cd /home/monvoir/app

# Посмотреть историю
git log --oneline -10

# Откатиться на нужный коммит (замените abc123)
sudo -u monvoir git checkout abc123
sudo ./update_vps.sh

# Вернуться на последнюю версию
sudo -u monvoir git checkout main
sudo ./update_vps.sh
```

### Где смотреть логи, если что-то не работает?

```bash
# Логи приложения
journalctl -u monvoir-app -f

# Логи Nginx
tail -f /var/log/nginx/monvoir_error.log

# Статус всех сервисов
systemctl status monvoir-app
systemctl status nginx
systemctl status postgresql
```

---

## 💡 Совет

Добавьте в закладки браузера:
- Этот файл: `START_HERE_GITHUB.md`
- Шпаргалку: `GITHUB_DEPLOY_CHEATSHEET.md`

---

## 🎓 Рекомендуемая последовательность

1. ✅ Прочитайте этот файл (вы здесь!)
2. ✅ Закоммитьте все файлы в GitHub
3. ✅ Запустите развертывание (2 команды выше)
4. ✅ Откройте приложение в браузере
5. ✅ Сохраните `GITHUB_DEPLOY_CHEATSHEET.md` под рукой
6. ✅ При обновлениях - используйте workflow из шпаргалки

---

**Готовы начать? Выполните команды из раздела "Что делать прямо сейчас" ⬆️**

Удачи! 🚀
