# 📝 Скрипты для управления VPS развертыванием

Этот проект включает набор скриптов для автоматизации развертывания и управления приложением на VPS.

---

## 🚀 Основные скрипты

### `deploy_vps.sh` - Автоматическое развертывание

**Назначение:** Полная автоматическая установка приложения на чистый VPS Ubuntu 22.04.

**Использование:**
```bash
sudo ./deploy_vps.sh
```

**Что делает:**
- ✅ Устанавливает все необходимые пакеты (PostgreSQL, Node.js, Python, Nginx)
- ✅ Создает базу данных PostgreSQL
- ✅ Настраивает пользователя приложения
- ✅ Устанавливает зависимости (npm, pip)
- ✅ Собирает frontend
- ✅ Настраивает права доступа для Nginx
- ✅ Создает и запускает systemd сервис
- ✅ Настраивает Nginx как reverse proxy
- ✅ Настраивает firewall (UFW)
- ✅ Опционально загружает тестовые данные

**Когда использовать:** При первом развертывании на новый VPS.

---

### `update_vps.sh` - Обновление приложения

**Назначение:** Обновление уже развернутого приложения.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./update_vps.sh
```

**Что делает:**
- ✅ Создает резервную копию базы данных
- ✅ Обновляет код из Git (если используется)
- ✅ Устанавливает новые зависимости
- ✅ Пересобирает frontend
- ✅ Обновляет права доступа
- ✅ Перезапускает приложение
- ✅ Очищает старые резервные копии

**Когда использовать:** После внесения изменений в код.

---

### `fix_permissions.sh` - Исправление прав доступа

**Назначение:** Быстрое исправление проблем с правами доступа для Nginx.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

**Что делает:**
- ✅ Добавляет пользователя в группу www-data
- ✅ Настраивает права на родительские директории
- ✅ Настраивает права на dist/ и config/
- ✅ Перезапускает Nginx

**Когда использовать:**
- Если сайт показывает белую страницу
- Если в консоли браузера ошибки 403 Forbidden
- Если JS/CSS файлы не загружаются

---

### `backup_db.sh` - Резервное копирование БД

**Назначение:** Создание резервной копии базы данных PostgreSQL.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./backup_db.sh
```

**Что делает:**
- ✅ Создает резервную копию базы данных
- ✅ Сжимает резервную копию (gzip)
- ✅ Сохраняет в `/home/shopapp/app/backups/`
- ✅ Автоматически удаляет старые копии (оставляет последние 30)

**Когда использовать:**
- Перед важными обновлениями
- Для регулярного резервного копирования
- Перед изменением структуры БД

---

### `restore_db.sh` - Восстановление БД

**Назначение:** Восстановление базы данных из резервной копии.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./restore_db.sh
```

**Что делает:**
- ✅ Показывает список доступных резервных копий
- ✅ Позволяет выбрать нужную копию
- ✅ Останавливает приложение
- ✅ Восстанавливает базу данных
- ✅ Запускает приложение

**Когда использовать:**
- При необходимости отката к предыдущему состоянию БД
- После неудачного обновления
- При восстановлении после сбоя

---

### `setup_domain.sh` - Настройка домена

**Назначение:** Подключение собственного доменного имени к сайту.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./setup_domain.sh
```

**Что делает:**
- ✅ Запрашивает доменное имя
- ✅ Обновляет конфигурацию Nginx
- ✅ Настраивает server_name для домена
- ✅ Перезапускает Nginx

**Когда использовать:**
- После настройки DNS записей (A-запись)
- Когда хотите использовать свой домен вместо IP

**Примечание:** Сначала настройте DNS записи у регистратора!

---

### `setup_ssl.sh` - Установка SSL сертификата

**Назначение:** Установка бесплатного SSL сертификата от Let's Encrypt.

**Использование:**
```bash
cd /home/shopapp/app
sudo ./setup_ssl.sh
```

**Что делает:**
- ✅ Устанавливает Certbot
- ✅ Получает SSL сертификат от Let's Encrypt
- ✅ Автоматически настраивает Nginx для HTTPS
- ✅ Настраивает автоматическое обновление сертификата

**Когда использовать:**
- После настройки домена (setup_domain.sh)
- **Обязательно для Telegram Mini Apps!** (требуют HTTPS)

**Примечание:** Домен должен уже резолвиться в ваш IP!

---

## 📋 Быстрая справка

| Задача | Команда |
|--------|---------|
| Первая установка | `sudo ./deploy_vps.sh` |
| Обновление кода | `cd /home/shopapp/app && sudo ./update_vps.sh` |
| Исправить права | `cd /home/shopapp/app && sudo ./fix_permissions.sh` |
| Настроить домен | `cd /home/shopapp/app && sudo ./setup_domain.sh` |
| Установить SSL | `cd /home/shopapp/app && sudo ./setup_ssl.sh` |
| Бэкап БД | `cd /home/shopapp/app && sudo ./backup_db.sh` |
| Восстановить БД | `cd /home/shopapp/app && sudo ./restore_db.sh` |

---

## 🔧 Дополнительные команды

### Управление приложением

```bash
# Статус приложения
systemctl status shop-app

# Перезапуск
systemctl restart shop-app

# Остановка
systemctl stop shop-app

# Запуск
systemctl start shop-app

# Логи в реальном времени
journalctl -u shop-app -f
```

### Управление Nginx

```bash
# Статус Nginx
systemctl status nginx

# Перезапуск Nginx
systemctl restart nginx

# Проверка конфигурации
nginx -t

# Логи ошибок
tail -f /var/log/nginx/shop_error.log

# Логи доступа
tail -f /var/log/nginx/shop_access.log
```

---

## 🆘 Решение частых проблем

### Проблема: Белая страница / 403 Forbidden

**Решение:**
```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

### Проблема: Приложение не запускается

**Решение:**
```bash
# Проверить логи
journalctl -u shop-app -n 100

# Попробовать запустить вручную
cd /home/shopapp/app
source venv/bin/activate
python3 app.py
```

### Проблема: База данных не подключается

**Решение:**
```bash
# Проверить PostgreSQL
systemctl status postgresql

# Проверить настройки
cat /home/shopapp/app/.env
```

---

## 📚 Полная документация

- **Быстрый старт**: `QUICK_START_RU.md` ⭐
- **Подключение домена**: `DOMAIN_SETUP_RU.md` 🌐
- **Развертывание из GitHub**: `DEPLOY_FROM_GITHUB.md`
- **Подробная инструкция**: `DEPLOY_TO_VPS_README.md`
- **Руководство по развертыванию**: `VPS_DEPLOY_GUIDE.md`

---

## ✅ Все скрипты готовы к использованию

Все скрипты уже имеют права на выполнение (`chmod +x`). Просто запускайте их с правами root (`sudo`).

**Вопросы?** Смотрите полную документацию выше! 🚀
