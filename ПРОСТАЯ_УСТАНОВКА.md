# Установка одной командой

## Самый простой способ развернуть на VPS

### Шаг 1: Подключитесь к VPS
```bash
ssh root@ВАШ_IP
```

### Шаг 2: Запустите установку
```bash
curl -fsSL https://raw.githubusercontent.com/ВАШ_ПОЛЬЗОВАТЕЛЬ/ВАШ_РЕПО/main/simple_deploy.sh | sudo bash
```

Скрипт спросит:
1. Откуда взять код? (GitHub или локальная директория)
2. URL вашего GitHub репозитория (если выбрали GitHub)
3. Имя пользователя для приложения (по умолчанию: shopapp)
4. Пароль для базы данных
5. Ваш домен (необязательно, для SSL)

**База данных, сервисы и настройки настроятся автоматически!**  

**Важно:** Категории и товары НЕ добавляются автоматически - вы добавите их сами через базу данных после установки.

---

## Что делает скрипт?

✅ Устанавливает все зависимости (Python, Node.js, PostgreSQL, Nginx)  
✅ Создает базу данных с безопасным паролем  
✅ Собирает фронтенд  
✅ Настраивает автозапуск приложения  
✅ Запускает сайт  

---

## После установки

### Ваш сайт доступен:
- **Без домена**: `http://ВАШ_IP`
- **С доменом**: `http://ВАШ_ДОМЕН`

### Полезные команды:
```bash
# Статус приложения
systemctl status shop-app

# Просмотр логов
journalctl -u shop-app -f

# Перезапуск
systemctl restart shop-app

# Просмотр настроек БД
cat /home/shopapp/app/.env
```

---

## SSL сертификат (HTTPS)

Если указали домен, установите бесплатный SSL:
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d ВАШ_ДОМЕН
```

Готово! Ваш сайт теперь доступен по `https://ВАШ_ДОМЕН`

---

## Обновление кода

```bash
# На VPS
cd /home/shopapp/app
sudo -u shopapp git pull
sudo -u shopapp npm run build
systemctl restart shop-app
```

---

## Часто задаваемые вопросы

**Q: Что если у меня нет GitHub?**  
A: Выберите вариант "2" при установке и скрипт использует текущую директорию

**Q: Как добавить товары?**  
A: Подключитесь к базе данных:
```bash
sudo -u postgres psql shop_db
INSERT INTO products (name, description, price, images, category_id) 
VALUES ('Товар', 'Описание', 1000, ARRAY['url.jpg'], 'category-id');
```

**Q: Как изменить настройки магазина?**  
A: Отредактируйте файл `/home/shopapp/app/config/settings.json`

---

**Готово! Ваш магазин работает! 🎉**
