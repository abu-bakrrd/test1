# 🚀 Инструкция по деплою на Render

## Подготовка проекта

Ваше приложение **готово для деплоя** на Render как Web Service! 

### ✅ Что уже настроено:
- Flask приложение с Gunicorn (production-ready)
- Собранный React фронтенд в `dist/public`
- PostgreSQL подключение через `DATABASE_URL`
- Production start скрипт без автоматического seed

---

## 📋 Шаги для деплоя

### 1. Создайте PostgreSQL базу данных на Render

1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   - **Name**: `flowery-bloom-db` (или любое имя)
   - **Region**: выберите ближайший к вам
   - **Plan**: Free (для теста) или Starter
4. Нажмите **"Create Database"**
5. **Важно!** Скопируйте **Internal Database URL** (он понадобится для Web Service)

---

### 2. Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите свой GitHub/GitLab репозиторий
3. Заполните настройки:

#### **Basic Settings:**
- **Name**: `flowery-bloom` (или любое имя)
- **Region**: тот же, что и у БД
- **Branch**: `main` (или ваша основная ветка)
- **Root Directory**: оставьте пустым
- **Runtime**: `Python 3`

#### **Build & Deploy Settings:**
- **Build Command**:
  ```bash
  pip install -r requirements.txt
  ```
  
- **Start Command**:
  ```bash
  gunicorn app:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120
  ```

#### **Plan:**
- **Instance Type**: Free (для теста) или Starter

---

### 3. Настройте Environment Variables

В разделе **Environment** добавьте:

| Key | Value |
|-----|-------|
| `DATABASE_URL` | *Internal Database URL из шага 1* |
| `PYTHON_VERSION` | `3.11` |

**Важно:** `DATABASE_URL` должен быть в формате:
```
postgresql://user:password@host:5432/database?sslmode=require
```

---

### 4. Инициализация базы данных

После первого успешного деплоя, нужно **один раз** создать таблицы и засеять данные:

#### Вариант A: Через Render Shell (рекомендуется)
1. В дашборде вашего Web Service нажмите **"Shell"**
2. Выполните команды:
   ```bash
   # Создать таблицы
   python init_tables.py
   
   # Засеять тестовые данные (опционально)
   python seed_db.py
   ```

#### Вариант B: Локально через DATABASE_URL
1. Скопируйте External Database URL из настроек БД
2. Локально выполните:
   ```bash
   export DATABASE_URL="postgresql://..."
   python init_tables.py
   python seed_db.py
   ```

---

## 🔧 Если используете свою БД (не Render PostgreSQL)

Если у вас уже есть PostgreSQL база данных (например, на AWS, Neon, или другом хостинге):

1. В **Environment Variables** добавьте:
   ```
   DATABASE_URL=postgresql://your_user:your_password@your_host:5432/your_database?sslmode=require
   ```

2. Убедитесь, что:
   - База данных доступна из интернета
   - В connection string есть `?sslmode=require`
   - Firewall разрешает подключения с IP адресов Render

3. Инициализируйте БД через Render Shell или локально (см. шаг 4 выше)

---

## 📝 Важные файлы проекта

### `start_production.sh`
Production скрипт запуска (используется Render):
```bash
gunicorn app:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120
```

### `init_tables.py`
Создает таблицы в БД (запускать вручную один раз):
```bash
python init_tables.py
```

### `seed_db.py`
Добавляет тестовые данные (запускать вручную при необходимости):
```bash
python seed_db.py
```

---

## ✅ Проверка деплоя

После деплоя:

1. Откройте URL вашего приложения (формат: `https://your-app.onrender.com`)
2. Проверьте:
   - ✅ Загружается главная страница
   - ✅ Отображаются товары (если засеяли данные)
   - ✅ API endpoints работают: `/api/categories`, `/api/products`

---

## 🐛 Troubleshooting

### Проблема: "Application failed to respond"
- Проверьте логи в Render Dashboard
- Убедитесь, что `DATABASE_URL` правильно настроен
- Проверьте, что порт `$PORT` используется в start команде

### Проблема: "No module named 'gunicorn'"
- Убедитесь, что в Build Command указано: `pip install -r requirements.txt`
- Проверьте, что файл `requirements.txt` существует в корне проекта

### Проблема: "Database tables don't exist"
- Выполните `python init_tables.py` через Render Shell
- Или запустите локально с правильным `DATABASE_URL`

### Проблема: "No products displayed"
- База данных пустая, выполните: `python seed_db.py`

---

## 📊 Мониторинг

- **Логи**: Render Dashboard → Logs
- **Metrics**: Render Dashboard → Metrics
- **Shell**: Render Dashboard → Shell (для команд в продакшене)

---

## 🔄 Обновления

При изменении кода:
1. Сделайте commit и push в GitHub/GitLab
2. Render автоматически пересоберет и задеплоит приложение
3. Не нужно запускать `seed_db.py` повторно - данные сохранятся

---

## 📞 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Python on Render](https://render.com/docs/deploy-flask)
- [PostgreSQL on Render](https://render.com/docs/databases)

---

**Готово!** 🎉 Ваше приложение задеплоено и работает в продакшене!
